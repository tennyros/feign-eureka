server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tennyros/feign-eureka-config.git
          default-label: master
          clone-on-start: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false